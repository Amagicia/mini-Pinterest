

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Upload Item</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen">
    <div class="bg-gray-800 p-8 rounded-xl shadow-lg w-full max-w-md">
        <h1 class="text-2xl font-bold mb-6 text-center">Upload Item</h1>
        <form action="/upload/save" method="POST" enctype="multipart/form-data" class="flex flex-col gap-4">
            
            <input type="text" name="name" placeholder="Title" required
                class="p-2 rounded bg-gray-700 text-white focus:outline-none" />
               


            <textarea name="description" placeholder="Description (optional)" rows="3"
                class="p-2 rounded bg-gray-700 text-white focus:outline-none"></textarea>

            <input type="file" name="imageFile" accept="image/*"
                class="p-2 rounded bg-gray-700 text-white focus:outline-none" />

            <input type="url" name="linkUrl" placeholder="Or paste a link (optional)"
                class="p-2 rounded bg-gray-700 text-white focus:outline-none" />

            <p class="text-xs text-gray-400">* You can upload either an image OR provide a link, not both</p>

            <button type="submit"
                class="bg-pink-500 hover:bg-pink-600 text-white py-2 rounded font-bold mt-2">Upload</button>
        </form>
    </div>
</body>
</html>
